# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib
*.log

# Test binary, built with `go test -c`
*.test
*.tmp

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# IDE files
.vscode
.DS_Store
.idea

# Misc
*.fiber.gz
*.fasthttp.gz
*.pprof
*.workspace

# Dependencies
/vendor/
vendor/
vendor
/Godeps/
/assets

# Customs
*.env*
.air*
/tmp
*.pem
apikey.txt
*_test.go

# gitignore เอาไว้เพื่อไม่ให้เกิดการ commit ไฟล์ที่เราไม่ต้องการ หรือ ไฟล์ที่เราไม่ต้องการให้เข้าไปอยู่ใน git repository